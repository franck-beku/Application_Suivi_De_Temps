namespace Application_Suivi_De_Temps.Models.Enums;

public enum ThresholdType
{
    Daily = 1,      // Seuil quotidien (par défaut)
    Weekly = 2      // Seuil hebdomadaire (pour évolutions futures)
}